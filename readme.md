[**Telepot**](https://telepot.readthedocs.io/en/latest/index.html): python framework for telegram bot

inline query example

# How to start
## build image
`docker build -t postergirl .`
## start container
`docker run --name ohmygod -e token=tokenhere -d postergirl`
